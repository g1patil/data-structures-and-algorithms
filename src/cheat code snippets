
String test = "teststring";

==> Get the substring
String sub = test.substring(0,5);
- in above 0 in inclusive and 5 is exclusive

==> Get the index of the char
String test = "jivan";
test.indexOf('v') => 2

==> If the char are from 0 to 25 for a to z then
convert each char to its number or index mapping
Substract 'a' from each char

char - 'a' ;
int[] arr = new int[26];
for (int i = 0; i < magazine.length(); i++) {
    array[ string.charAt(i) - 'a' ]++;
}

==> convert the char array to string
char[] chars = new char[]{'a','b','c'};
String s = String.valueOf(chars);

==> Character API
Character.isDigit('0');
Character.toUpperCase('a');
Character.toLowerCase('A');